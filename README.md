##  📌 Descripción de las partes clave del código:
  - **#include <stdio.h>**: Incluye la biblioteca estándar de entrada y salida, necesaria para usar funciones como printf.
int main(): Función principal del programa, punto de entrada de la ejecución. Devuelve un entero para indicar el estado de terminación al sistema operativo.
int n = 30;: Declaración e inicialización de una variable entera n con valor 30. Esta variable define el límite superior del bucle.
int i;: Declaración de una variable entera i que se utilizará como contador del bucle for.
for (i = 0; i < n; i++): Bucle for que inicializa i a 0, verifica si i es menor que n en cada iteración, y aumenta i en 1 después de cada ciclo. El bucle se ejecuta 30 veces, con i tomando valores de 0 a 29.
printf("el valor actual de i es %d\n", i);: Imprime el valor actual de i en cada iteración del bucle. El especificador %d se utiliza para imprimir valores enteros, y \n inserta un salto de línea.
printf("el valor actual de i es %d\n", i);: Después de que el bucle termina, se imprime el valor de i, que ahora es 30, ya que el incremento final del bucle lo lleva a este valor.
return 0;: Indica que el programa se ha ejecutado correctamente. El valor 0 es convencionalmente usado para indicar éxito.

## 🖥️ Tecnologías Utilizadas:

- C
- Visual Studio Code

## 📦 Requisitos:

- Compilador gcc o cc.

## ⚙️ Compilación

- Compilación en sistemas GNU/Linux es el siguiente comando: **gcc for.c -o for**.
- En entornos Windows con Microsoft Visual C++, se utiliza el compilador cl.exe. Para compilar un programa, se debe abrir una ventana de símbolo del sistema para desarrolladores y ejecutar un comando como: **for.c**. Esto genera un archivo ejecutable llamado **for.exe**.

</br>

💙 <strong>Alejandra Contreras</strong></br></br>
<a href="https://www.linkedin.com/in/alejandraconb-dev/" target="_blank">
<img align="left" src="https://img.shields.io/badge/-LinkedIn-%230077B5?style=for-the-badge&logo=linkedin&logoColor=white" target="_blank"></a>
<img align="center" src="https://raw.githubusercontent.com/Tarikul-Islam-Anik/Animated-Fluent-Emojis/master/Emojis/Smilies/Relieved%20Face.png" target="_blank" height="40"></a>
